apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-console
  namespace: conduktor
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - console.k8s.tutorial
      secretName: console-tls
  rules:
    - host: console.k8s.tutorial
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: console
                port:
                  number: 80
